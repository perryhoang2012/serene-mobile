import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const SleepSounds = (props: Props) => {
  return (
    <View>
      <Text>SleepSounds</Text>
    </View>
  );
};

export default SleepSounds;

const styles = StyleSheet.create({});
